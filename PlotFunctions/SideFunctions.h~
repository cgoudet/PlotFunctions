#ifndef SIDEFUNCTIONS_H
#define SIDEFUNCTIONS_H

#include "TH1D.h"
#include "TTree.h"
#include <vector>
#include <string>
#include <iostream>
#include <fstream>
#include "boost/multi_array.hpp"
using boost::multi_array;
using boost::extents;


using std::fstream;
using std::string;
using std::vector;
using std::cout;
using std::endl;
/**\brief Compute Chi2 of two matching histograms
   \param MCHist Histogram with MC role
   \param DataHist Histogram with Data role
   \return Chi2/ndf
 */
double ComputeChi2( TH1D *MCHist, TH1D *DataHist );

TTree* Create4DTree( vector< vector< vector< vector< double > > > > const &vect , string treeName);
void Read4DTree(vector< vector< vector< vector< double > > > > &inVect, TTree* inTree );
//int Dirac( unsigned int i, unsigned int j);
int FindFitBestRange( TH1D *hist, int &binMin, int &binMax, double chiMinLow=9, double chiMinUp=25);
void WriteLatexMinipage( fstream &latexStream, vector<string> vect, unsigned int nPlotPerWidth=0 );
void WriteLatexHeader( fstream &latexStream, string author );
#include "Template/PlotFunctionsTpp.h"
string StripString( string &inString, bool doPrefix=1, bool doSuffix=1 );
void RemoveExtremalEmptyBins( TH1 *hist );

#endif
